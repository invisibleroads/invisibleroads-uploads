$('.btn-file input').fileupload({
	dataType: 'json',
	done: function(e, d) {
    var $o = $(this).parent('span'), upload = d.result;
    $o.data('upload_id', upload.id)
      .trigger('uploaded.ir.upload', upload);
	}
});
